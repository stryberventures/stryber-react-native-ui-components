import React from 'react';
import {View} from 'react-native';

import Input from './components/Input';

export default () => (
  <View
    style={{
      flex: 1,
      marginTop: 200,
      marginHorizontal: 20,
    }}>
    <Input />
  </View>
);
