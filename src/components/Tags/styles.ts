import {StyleSheet} from 'react-native';

const getStyles = () => {
  return StyleSheet.create({
    container: {
      flexDirection: 'row',
      flexWrap: 'wrap'
    },
  });
};
export default getStyles;
